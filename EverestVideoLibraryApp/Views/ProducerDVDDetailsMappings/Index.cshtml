@model IEnumerable<EverestVideoLibraryApp.Models.ProducerDVDDetailsMapping>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DVDDetail.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Producer.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DVDDetail.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Producer.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.proDVDmapping }) |
            @Html.ActionLink("Details", "Details", new { id=item.proDVDmapping }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.proDVDmapping })
        </td>
    </tr>
}

</table>
