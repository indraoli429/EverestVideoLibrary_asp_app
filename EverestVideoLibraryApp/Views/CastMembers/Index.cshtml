@model IEnumerable<EverestVideoLibraryApp.Models.CastMember>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Artist.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DVDDetail.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Artist.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DVDDetail.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CastMemberId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CastMemberId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CastMemberId })
        </td>
    </tr>
}

</table>
